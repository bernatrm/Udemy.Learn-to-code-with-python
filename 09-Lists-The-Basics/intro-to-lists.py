empty = [] # All objects are same type, not mandatory but recomended
empty = list() # Only when converting from oder type os object

sodas = ["Coke", "Pepsi", "Dr. Peper"] # 3 elements in determinated order
print(len(sodas)) # Elements in the list

quarterly_reveneus = [15000, 12000, 9000, 20000]
print(len(quarterly_reveneus))

stock_prices = [343.26, 89.25]
print(len(stock_prices))

user_settings = [True, False, False, True, False]
print(len(user_settings))
