employee = ("Bob", "Johnson", "Manager", "50")

# first_name = employee[0]
# last_name = employee[1]
# position = employee[2]
# age = employee[3]

# first_name, last_name, position, age = employee
# print(first_name, last_name, position, age)

# subject, verb, adjective = ["Python", "is", "fun"]
# print(subject)
# print(verb)
# print(adjective)

# first_name, last_name, title = employee # we need same number of variables that elements on the tuple
# first_name, last_name, title, position, age, salary = employee # we need same number of variables that elements on the tuple

a = 5
b = 10

a, b = (b, a)

print (a)
print (b)
